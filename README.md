# nfckeyboard

Read NFC tags and type the extracted Imagotag serial as keyboard input.

## Usage

Install dependencies:

```bash
uv sync
```

Run in system tray mode (default):

```bash
uv run -m nfckeyboard
```

Run in interactive terminal mode:

```bash
uv run -m nfckeyboard --interactive
```
